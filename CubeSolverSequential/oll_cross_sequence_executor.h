#pragma once

#include "notation_translator.h"
#include "f2l_edge_rotations.h"

extern void executeOLLCrossSequence(const int cubeIdx, const uint2 crossIdx, const uint2 cornerIdx, const uint2 edgeIdx, const Notation sequence[], const int idx);