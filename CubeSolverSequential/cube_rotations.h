#pragma once

#include "variables.h"

extern void turnLayer(const int cubeIdx, const CubeLayer cubeLayer, const Direction direction, const bool twice);
extern void turnCube(const int cubeIdx, const Direction direction, const bool twice);