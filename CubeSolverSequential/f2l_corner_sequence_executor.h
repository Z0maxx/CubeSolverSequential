#pragma once

#include "f2l_corner_rotations.h"
#include "notation_translator.h"

extern void executeF2LCornerSequence(const int cubeIdx, const uint2 crossIdx, const uint2 cornerIdx, const Notation* sequence, const int idx);